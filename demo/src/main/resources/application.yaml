spring:
  application:
    name: Student

  datasource:
    url: jdbc:h2:mem:Student
    driver-class-name: org.h2.Driver
    username: matezepam
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8989